interface:
  display_name: "發包標案每日搜尋"
  short_description: "Daily FABAO CLUB tender search"
  default_prompt: "Use $fclub-tender-leads to check today's FABAO CLUB tender cases and update confirmed workflow rules."

policy:
  allow_implicit_invocation: true
